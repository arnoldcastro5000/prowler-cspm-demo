map_hash_bucket_size 128;

map $http_x_cf_secret $cf_secret_valid {
    default                 0;
    "${CF_ACCESS_SECRET}"   1;
}

server {
    listen 8080;

    if ($cf_secret_valid = 0) { return 403; }

    root /usr/share/nginx/html;
    index index.html;

    # Performance settings
    sendfile        on;
    tcp_nopush      on;
    tcp_nodelay     on;
    keepalive_timeout 65;

    # Security headers applied in every location block
    # (nginx add_header in location overrides server-level, so must repeat)

    # HTML — always revalidate, serve pre-compressed if available
    location / {
        gzip_static on;
        try_files $uri $uri/ /index.html;
        add_header Cache-Control                "no-cache, must-revalidate"                always;
        add_header X-Content-Type-Options       "nosniff"                                  always;
        add_header X-Frame-Options              "DENY"                                     always;
        add_header Referrer-Policy              "strict-origin-when-cross-origin"          always;
        add_header Strict-Transport-Security    "max-age=31536000; includeSubDomains"      always;
        add_header Permissions-Policy           "camera=(), microphone=(), geolocation=(), payment=()" always;
    }

    # JS and CSS — immutable, cache 1 year, serve pre-compressed if available
    location ~* \.(js|css)$ {
        gzip_static on;
        try_files $uri =404;
        add_header Cache-Control                "public, max-age=31536000, immutable"      always;
        add_header X-Content-Type-Options       "nosniff"                                  always;
        add_header X-Frame-Options              "DENY"                                     always;
        add_header Referrer-Policy              "strict-origin-when-cross-origin"          always;
        add_header Strict-Transport-Security    "max-age=31536000; includeSubDomains"      always;
        add_header Permissions-Policy           "camera=(), microphone=(), geolocation=(), payment=()" always;
    }

    # Findings JSON — revalidate on each request, no pre-compression
    location ~* \.json$ {
        try_files $uri =404;
        add_header Cache-Control                "no-cache, must-revalidate"                always;
        add_header X-Content-Type-Options       "nosniff"                                  always;
        add_header X-Frame-Options              "DENY"                                     always;
        add_header Referrer-Policy              "strict-origin-when-cross-origin"          always;
        add_header Strict-Transport-Security    "max-age=31536000; includeSubDomains"      always;
        add_header Permissions-Policy           "camera=(), microphone=(), geolocation=(), payment=()" always;
    }
}
