name: Python Lint

on:
  push:
    branches: [main]
    paths:
      - 'ingest/**'
  pull_request:
    paths:
      - 'ingest/**'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - run: pip install ruff

      - name: Lint
        run: ruff check ingest/
