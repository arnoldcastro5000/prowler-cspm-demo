name: Secret Scan

on:
  push:
  pull_request:

permissions: {}

jobs:
  betterleaks:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Install betterleaks
        env:
          BL_VERSION: "1.4.0"
        run: |
          curl -fsSL -o betterleaks.tar.gz \
            "https://github.com/betterleaks/betterleaks/releases/download/v${BL_VERSION}/betterleaks_${BL_VERSION}_linux_x64.tar.gz"
          echo "$(curl -fsSL "https://github.com/betterleaks/betterleaks/releases/download/v${BL_VERSION}/betterleaks_${BL_VERSION}_checksums.txt" \
            | grep linux_x64.tar.gz | awk '{print $1}')  betterleaks.tar.gz" | sha256sum --check
          tar -xzf betterleaks.tar.gz betterleaks
          sudo mv betterleaks /usr/local/bin/

      - name: Scan for secrets
        run: betterleaks scan --source git --max-depth 999999
